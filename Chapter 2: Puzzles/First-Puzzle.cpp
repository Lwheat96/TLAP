#include <iostream>
#include <vector>

int main() {
    for (int hashNum = 1; hashNum <= 5; hashNum++) {
        std::cout << "#";
    }
    std::cout << "\n";
    std::cout << "The first line is showing correctly.\n";
}